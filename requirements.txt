# Road Damage Detection — Python dependencies
# Tested on Google Colab (Python 3.12, Tesla T4) and Streamlit Community Cloud.
# Pinned to floors that are known to expose the YOLO26 family. OpenCV is pulled
# in by ultralytics; on Streamlit Cloud it needs libgl1 (see packages.txt).

ultralytics>=8.4.0      # YOLO26 / YOLOv8 detector + training + validation
torch>=2.2              # provided by Colab; listed for completeness
opencv-python>=4.9      # image IO and box drawing
numpy>=1.26
pandas>=2.0             # metric / detection tables
matplotlib>=3.7         # EDA and result plots
seaborn>=0.13           # nicer statistical plots in EDA
scikit-learn>=1.3       # confusion-matrix helpers / split sanity checks
Pillow>=10.0
streamlit>=1.32         # live demo dashboard (Colab tunnel + Streamlit Cloud)
tqdm>=4.66
PyYAML>=6.0
